Graduate Admission Prediction (TensorFlow)
This project uses TensorFlow and a neural network model to predict the probability of graduate school admission based on applicant features.
The model is trained using a dataset containing GRE scores, GPA, and university rank.
